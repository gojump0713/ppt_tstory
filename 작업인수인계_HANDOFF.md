# 작업 인수인계 (2026-09-11 최종)

다른 계정/세션에서 이어서 작업할 때 이 문서부터 읽으세요. 이전 세션의 대화 메모리는 이어지지 않습니다.

## 1. 프로젝트 개요
- 목적: 공공도서관 담당 공무원·관리자 대상 **「AI 시민 동화책 제작 플랫폼」 도입 제안** 인터랙티브 HTML 발표자료 (발표자: 틸론)
- 산출물: `presentation/` (index.html · css/presentation.css · js/data.js · js/slides.js · js/app.js · media/ · tools/)
- 실행: `presentation/index.html` 더블클릭 → `F` 전체화면

## 2. 정본 문서 (반드시 숙지 후 작업)
| 문서 | 역할 |
|---|---|
| `AI시민동화책_공공도서관_발표멘트_최종.md` | **콘텐츠 1차 기준.** 낭독 멘트·수치·출처 |
| `AI시민동화책_인터랙티브HTML_작업지시서_PRD.md` | 18장 구조, 수치 무결성 규칙, UX·모션 원칙 |
| `AI시민동화책_인터랙티브HTML_화면설계서.md` | 장표별 제목·거버닝 메시지·화면 문구·레이아웃 |
| `AI시민동화책_인터랙티브HTML_Higgsfield_프롬프트.md` | 장표별 배경 영상/이미지 프롬프트·파일명 규칙 |

## 3. 현재 상태 (완료)
- 2026-09-15: PART Ⅱ(19~25장, `reading-activation-16x9.html` 기반 독서 활성화 방안) 추가 → 총 26장. 시연은 18·26장 DEMO 버튼/D 키로 열고 연 장표로 복귀. 장표 밖 크롬(챕터·쪽번호)은 배경 미디어가 실제로 뜬 장표에서만 밝은 글자(`html.cur-media`)
- 18장 + 시연 5화면 구현 완료. 기능 QA 전 항목 통과, 페이지 오류 0건, 넘침·잘림 0건
- **배경 미디어 27건 제작 완료** (Higgsfield MCP, 약 560크레딧)
  - 영상 9건 `seedance_2_5` 16:9 7~8초 · 이미지 9건 + 포스터 9건 `gpt_image_2` 16:9 2K
  - 전부 `presentation/media/` 에 문서 규칙 파일명으로 저장, 1920×1080 JPG 250~500KB
- **배경 미디어 강조 모드**(기본 ON, `B` 키로 해제): 어두운 베일 위에 배경 영상이 드러나고, 배경 위 글자는 흰색, 흰 카드 안 글자는 원래 색
  - 구현 방식: 클래스마다 색을 지정하지 않고 `--ink/--ink-2/--muted` 토큰을 문맥별로 재정의해 상속으로 해결 (CSS 맨 끝 블록)
- **글자 크기**: 렌더링 실측 기준 18px 미만 제거, 본문급 22px 확보 (`tools/audit_type.js` 로 재검증 가능)
- **목차**: `T`·`O`·`Esc` 키 또는 우하단 페이지 번호 클릭 → 18장 + 시연 항목 19개, 현재 장표 강조, 클릭 이동, Esc 닫기
- `Esc`는 시연 중이면 발표 복귀 · 패널이 열려 있으면 닫기 · 그 외에는 목차 열기
- **GitHub**: `gojump0713/ppt_tstory` · `main` 푸시 시 `.github/workflows/deploy.yml`이 `presentation/`을 Pages 사이트 루트로 게시 → https://gojump0713.github.io/ppt_tstory/
- **링크 공유(Open Graph)**: `presentation/og-image.jpg`(1200×630, 18장 배경의 마법 동화책 + 제목)와 `index.html` head 의 og/twitter 태그
  - 카드 원본은 `tools/og.html` · 생성은 `node tools/make_og.js` → PIL 로 JPG 변환 (`tools/README.md`)
  - 이미지 교체 시 `og:image` 의 `?v=` 를 올리고 카카오 공유 디버거에서 캐시 초기화 필요
- **파비콘**: `favicon.svg`(원본) → `favicon.ico`(16/32/48) · `apple-touch-icon.png`(180) · 생성은 `node tools/make_icons.js`
- 시작 시 단축키 토스트 1회 노출 (`T·O·Esc 목차 · S 출처 · P 발표자 메모 · F 전체화면 · D 시연`)

## 4. 이번 세션에서 고친 것
- 5장: 하단 문장과 출처 줄 완전 겹침 해소, `50%` 크기·색 위계 정상화(선택자 `.venn .circ span` → `> span`)
- 8장: 중앙 알약이 컨테이너 폭에 눌려 아이콘이 잘리던 문제(`width:max-content`)
- 11·13·18장: flex `gap`이 문장 중간 강조 태그를 떼어놓아 조사가 분리되던 버그 (텍스트를 `<span>`으로 감쌈)
- 14장: 우측 카드가 페이지 번호와 겹침 → 중복 문장 제거로 공간 확보
- 17장: 내부 지시문 `사업계획 확정 후 숫자 입력` 노출 삭제, 원고에 없는 4번째 방문 목적 `전시·나눔` 삭제
- 4장: 오타 `콘텍츠` → `콘텐츠`, 자리표시자처럼 반복되던 `전략분야` 10개 제거
- 2·18장: 낭독과 겹치는 시연 안내 문구 삭제 (사용자 지침)
- 7·15장: 원고를 그대로 옮긴 화면 문장·부연 삭제
- 3장: PRD가 요구한 주의 문구가 17px → 22px
- app.js: 영상 로드 실패 시 포스터 이미지로 폴백 (PRD 폴백 순서 준수)
- 시연 5화면: 5단계 사이드바 추가, 덱 조작 버튼을 제품 UI 밖으로 분리
- 제목·본문에 `text-wrap: balance / pretty` 적용해 마지막 줄 외톨이 단어 방지
- 글자 크기 일괄 상향: 렌더링 실측 13~21px → 최소 18px, 본문급 22px 이상
- 배경 미디어 강조 모드 도입 후 보완:
  - `--ink`를 배경색으로 쓰던 규칙 18곳을 `--ink-surface`로 분리 (어두운 알약이 흰색으로 뒤집혀 글자가 사라지던 회귀 해결 — 4·5·9·16장)
  - 연한 틴트 칩(AI 생성·사람의 선택 등)을 진한 틴트 + 흰 글자로 (대비 확보)
  - 배경이 특히 밝은 3·9장은 베일을 한 단계 더 진하게, 3장 수치군에는 국소 스크림
  - 히어로 소제목이 역광에 묻혀 흰색 + 그림자로 변경
- 15장 Q&A 카드 구분선 높이 정렬, 2장 좌우 카드 세로 중앙 정렬

## 5. 남은 일
1. 출처 3(독서율 50% 목표)·8(디지털포용법·AI 기본법)은 `status:"verify"` — 원문 대조 후 `verified`로 바꾸면 화면의 확인 표시가 사라집니다
2. `○○○권`: 사업계획 확정 후 `data.js`의 `METRICS.annualArchiveCount`에 숫자 입력 (임의 숫자 금지)
3. (선택) `DECK_META.demoUrl`에 실제 서비스 URL을 넣으면 내장 시연 대신 새 창으로 실제 서비스가 열립니다
4. (선택) 배경 영상이 마음에 들지 않는 장표는 Higgsfield 문서의 프롬프트로 재생성해 같은 파일명으로 덮어쓰면 즉시 반영됩니다

## 6. 검증 도구 (`presentation/tools`)
```bash
npm i puppeteer-core@23        # 최초 1회
node qa.js                     # 32장 순회 + 시연 + 스케일 + 넘침/잘림 → qa-shots/report.json
node shot.js 01 07 d1 d5       # 지정 장표 스크린샷
node shot_toc.js               # 목차 화면 스크린샷·동작 확인
node audit_type.js             # 화면에 실제 렌더된 22px 미만 글자 목록
node shot_special.js           # 키 반복·IME·조합키 등 동작 검사
```

## 7. 조작키
`→ ↓ Space PgDn` 다음 · `← ↑ PgUp` 이전 · `Home/End` 처음/끝 · `T`·`O`·`Esc` 목차 · `S` 출처 · `P` 발표자 메모 · `F` 전체화면 · `M` 모션 최소화 · `B` 배경 강조 토글 · `R` 다시 재생 · `D` 시연 (`Backspace/Esc` 복귀)
