# Tstory.ai 기관용 AI동화책 운영 가이드 — HTML 발표자료 PRD + 화면구성

- 문서 버전: v0.1 (2026.09.16)
- 원본: `Tstory_기관용_AI동화책_운영가이드_토큰.pptx` (6장)
- 제작 방식: 바이브코딩 (Cursor / Claude Code 등), 단일 HTML 파일

---

## 1. 개요

### 1.1 목적
PPT로 정리한 기관용 운영 가이드를 **웹 브라우저에서 바로 발표·공유할 수 있는 HTML 발표자료**로 전환한다. 정적인 슬라이드를 넘어, 담당자가 **인원과 책 형태를 직접 넣어 예산을 계산**해 볼 수 있게 한다.

### 1.2 대상
| 구분 | 사용 상황 | 핵심 니즈 |
|---|---|---|
| 발표자(틸론 영업·기획) | 도서관·문화센터 설명회, 대면·화상 발표 | 키보드로 넘기기, 전체화면, 발표자 노트 |
| 청중(기관 담당자·예산 담당자) | 발표 청취, 링크로 다시 보기 | 이용권·가격 이해, 우리 기관 예산 산출 |

### 1.3 성공 기준
- 링크 하나로 PC·태블릿·모바일에서 열린다.
- 발표 중 예산 계산기를 조작해 청중 기관 규모의 금액을 즉석에서 보여 줄 수 있다.
- 가격이 바뀌면 **설정값 한 곳만 수정**하면 모든 화면 숫자가 함께 바뀐다.

---

## 2. 범위

### 2.1 포함 (MVP)
1. 슬라이드 7장 (표지 + 본문 5장 + 마무리)
2. 슬라이드 넘기기: 키보드, 버튼, 스와이프
3. 진행 표시(페이지 번호, 진행 바)
4. 전체화면 전환
5. 예산 계산기 (인원 × 이용권 × 책 형태)
6. 발표자 노트 토글
7. 슬라이드 목록(개요) 보기
8. PDF 인쇄용 스타일(슬라이드 1장 = 인쇄 1페이지)

### 2.2 제외 (추후)
- 발표자 화면/청중 화면 분리 동기화
- 로그인, 문의 폼 서버 전송
- 다국어

---

## 3. 기술 요구사항

| 항목 | 결정 |
|---|---|
| 형태 | **단일 `index.html`** (CSS·JS 인라인) |
| 프레임워크 | 사용하지 않음 (Vanilla JS). 빌드 도구 없음 |
| 폰트 | Pretendard (jsDelivr CDN), 대체 폰트 `'Malgun Gothic', sans-serif` |
| 차트 | 라이브러리 없이 CSS 막대로 구현 (외부 의존성 최소화) |
| 화면 비율 | 16:9 캔버스 기준(1920×1080 설계), 뷰포트에 맞춰 스케일 |
| 브라우저 | Chrome, Edge, Safari 최신 버전 |
| 외부 이미지 | 사용하지 않음. 아이콘은 인라인 SVG |

---

## 4. 데이터 설계 (단일 설정값)

모든 가격·수치는 아래 객체 한 곳에서 관리하고, 화면은 이 값으로 렌더링한다. **HTML에 숫자를 직접 쓰지 않는다.**

```js
const CONFIG = {
  token: {
    name: '토큰',
    perImage: 10,          // 그림 1장당 토큰
    packSize: 1000,        // 토큰 묶음 단위
    packPrice: 15000       // 토큰 1000개 가격(원)
  },
  book: {
    generate: 16,          // 생성 횟수 (표지 포함)
    revise: 10             // 수정 횟수
    // 1권 토큰 = (16 + 10) × 10 = 260개 → 계산으로 산출
  },
  room: { minMembers: 3 },
  plans: {
    digital: {
      label: '디지털 이용권',
      price: 4000,
      items: ['동화책 1권 제작', 'PDF 동화책 다운로드', '오디오북 다운로드']
    },
    print: {
      label: '책제작 포함 이용권',
      items: ['디지털 이용권 전체 구성', '실물 동화책 1권', '인쇄·제본 후 기관으로 배송']
    }
  },
  books: [
    { id: 'insta',    label: '인스타북',            desc: '가볍고 부담 없는 정사각 소형 책', price: 18000 },
    { id: 'vertical', label: '세로형 포토북',        desc: '동화책다운 세로 판형',            price: 20000, badge: '기본', default: true },
    { id: 'zine',     label: '포토진',              desc: '잡지처럼 얇은 중철 제본',          price: 23000 },
    { id: 'square',   label: '정방형 포토북',        desc: '넓은 그림 면, 소장용',            price: 30000, badge: '프리미엄' }
  ],
  budgetExamples: [3, 10, 20, 30],
  notice: '가격 확정 전 초안이며, 부가세 포함 여부와 배송비 기준은 확정 후 반영합니다.'
};
```

파생값 규칙:
- 1권 토큰 수 = `(generate + revise) × perImage`
- 학급방 예산 = `인원 × 선택한 이용권 1인당 가격`
- 금액 표기 = `toLocaleString('ko-KR') + '원'`

---

## 5. 디자인 시스템

### 5.1 색상 (원본 PPT 계승)
| 토큰 | HEX | 용도 |
|---|---|---|
| `--c-dark` | `#244D3B` | 표지·마무리 배경, 큰 숫자 |
| `--c-green` | `#3E7B5C` | 주요 강조, 디지털 이용권 |
| `--c-mint` | `#E6F0EA` | 카드 배경 |
| `--c-coral` | `#E9785A` | 보조 강조, 책제작 포함 이용권, 주의 |
| `--c-coral-light` | `#FCE9E3` | 보조 카드 배경 |
| `--c-ink` | `#26302B` | 본문 |
| `--c-gray` | `#6B756F` | 설명·캡션 |
| `--c-line` | `#D5DED8` | 테두리 |

### 5.2 타이포 (1920×1080 기준)
| 요소 | 크기 | 굵기 |
|---|---|---|
| 표지 제목 | 88px | 800 |
| 슬라이드 제목 | 60px | 700 |
| 부제 | 30px | 400 |
| 큰 숫자 | 96~120px | 800 |
| 카드 제목 | 36px | 700 |
| 본문 | 28px | 400 |
| 캡션 | 22px | 400 |

### 5.3 공통 모티프
- 모서리 둥근 카드(`border-radius: 24px`)
- 번호가 들어간 원형 배지
- 디지털 = 초록, 책제작 = 코랄로 **색상 의미를 끝까지 일관되게** 유지
- 슬라이드 상단 가로 띠, 카드 한쪽 세로 줄 같은 장식선은 사용하지 않음

### 5.4 모션
- 슬라이드 전환: 가로 슬라이드 + 페이드, 400ms `ease-out`
- 슬라이드 진입 시 요소 순차 등장(80ms 간격)
- 숫자 카운트업: 큰 가격 숫자 600ms
- `prefers-reduced-motion` 설정 시 모든 애니메이션 끔

---

## 6. 공통 UI (모든 슬라이드)

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│                    [ 슬라이드 캔버스 16:9 ]                  │
│                                                           │
├───────────────────────────────────────────────────────────┤
│ ▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░ 진행 바                             │
│ Tstory.ai 기관용 운영 가이드    ◀  3 / 7  ▶   ☰  🗒  ⛶       │
└───────────────────────────────────────────────────────────┘
```

| 요소 | 동작 |
|---|---|
| ◀ ▶ | 이전·다음 슬라이드 |
| `3 / 7` | 현재 페이지 / 전체 |
| ☰ 개요 | 전체 슬라이드 썸네일 그리드 열기 |
| 🗒 노트 | 하단에 발표자 노트 패널 토글 |
| ⛶ 전체화면 | Fullscreen API |
| 하단 바 | 마우스 3초 미사용 시 자동 숨김, 움직이면 표시 |

### 6.1 키보드 단축키
| 키 | 동작 |
|---|---|
| `→` `Space` `PageDown` | 다음 |
| `←` `PageUp` | 이전 |
| `Home` / `End` | 처음 / 마지막 |
| `F` | 전체화면 |
| `O` | 개요 보기 |
| `N` | 발표자 노트 |
| `Esc` | 개요·노트 닫기 |
| 숫자 `1`~`7` + `Enter` | 해당 슬라이드로 이동 |

계산기 입력란에 포커스가 있을 때는 방향키·숫자 단축키를 비활성화한다.

### 6.2 URL
- 현재 슬라이드를 해시로 유지: `#/3`
- 새로고침·링크 공유 시 해당 슬라이드로 열림

---

## 7. 화면구성 (슬라이드별)

### S1. 표지

```
┌─────────────────────────────────────────────────────┐
│ ■ dark 배경                                          │
│                                                     │
│  도서관 · 문화센터 · 교육기관용                         │        (●)
│                                                     │    큰 원형 그래픽
│  Tstory.ai                                          │    (책 펼침 SVG)
│  AI동화책 운영 가이드                                  │           (●) 코랄
│                                                     │
│  학급방 만들기부터 이용권 가격, 실물 동화책 받기까지        │
│                                                     │
│  틸론(Tilon)                                          │
└─────────────────────────────────────────────────────┘
```
- 인터랙션: 없음. "→ 키로 시작" 안내를 하단에 흐리게 표시 후 3초 뒤 사라짐
- 노트: 발표 목적과 순서 소개

---

### S2. 서비스 흐름 — 아이가 작가가 되는 AI동화책

```
 아이가 작가가 되는 AI동화책
 이야기는 아이가 쓰고, 그림은 AI가 함께 그려요

 ┌── 디지털 이용권 (생성 16회 · 수정 10회) ─────────────┐ ┌ 책제작 포함 ┐
 │ ┌───────┐   ┌───────┐   ┌───────┐              │ │ ┌───────┐ │
 │ │  ①    │ → │  ②    │ → │  ③    │           →  │ │ │  ④    │ │
 │ │이야기   │   │그림     │   │다운로드 │              │ │ │실물    │ │
 │ │쓰기    │   │그리기   │   │        │              │ │ │동화책  │ │
 │ │설명     │   │설명     │   │설명     │              │ │ │설명     │ │
 │ └───────┘   └───────┘   └───────┘              │ │ └───────┘ │
 └──────────────────────────────────────────────────┘ └───────────┘
```
| 카드 | 제목 | 설명 |
|---|---|---|
| ① | 이야기 쓰기 | 주제와 인물을 정하고 아이가 직접 글을 써요 |
| ② | 그림 그리기 | 토큰을 사용하면 AI가 장면을 그려요 |
| ③ | 다운로드 | PDF 동화책과 오디오북을 받아요 |
| ④ | 실물 동화책 | 인쇄·제본된 책을 기관으로 배송해요 |

- 그룹 표시: ①~③을 초록 점선 박스 "디지털 이용권", ①~④ 전체를 코랄 박스 "책제작 포함 이용권"으로 감싼다.
- 인터랙션: 진입 시 카드가 ①→④ 순서로 등장. 그룹 라벨에 마우스를 올리면 해당 범위 카드만 강조, 나머지는 흐리게.
- 수치: "생성 16회 · 수정 10회"는 `CONFIG.book`에서 가져온다.

---

### S3. 학급방으로 한 번에 운영해요

```
 학급방으로 한 번에 운영해요
 담당자가 방을 만들고, 참여자는 링크로 들어와요

  ① 담당자 가입     기관 담당자 계정을 만들어요          ┌──────────────┐
  │                                                   │ 최소 인원      │
  ② 학급방 만들기   프로그램·기수별로 방을 만들어요       │              │
  │                                                   │   3명         │
  ③ 참여자 초대 ●   참여 링크를 공유해요 (최소 3명)       │              │
  │                                                   │ 참여자 3명 이상 │
  ④ 동화책 만들기   참여자가 각자 한 권씩 만들어요         │ 일 때 방 생성   │
  │                                                   └──────────────┘
  ⑤ 검토·완성       담당자가 작품을 확인하고 신청해요
```
- 좌측: 세로 타임라인(번호 원 + 연결선). ③은 코랄로 강조.
- 우측: dark 카드에 큰 숫자 `3명` (`CONFIG.room.minMembers`)
- 인터랙션: `→` 키를 누를 때마다 단계가 하나씩 켜지는 **단계 공개 모드**(1~5). 5단계까지 모두 켜진 뒤 `→`를 누르면 다음 슬라이드로 이동.

---

### S4. 이용권은 두 가지예요

```
 이용권은 두 가지예요
 참여자 1인당 가격

 ┌─────────────────────────┐   ┌─────────────────────────┐
 │ 디지털 이용권 (초록)        │   │ 책제작 포함 이용권 (코랄)    │
 │                         │   │                         │
 │ 4,000원                  │   │ 20,000원~                │
 │                         │   │                         │
 │ • 동화책 1권 제작           │   │ • 디지털 이용권 전체 구성     │
 │   토큰 260개              │   │ • 실물 동화책 1권           │
 │   (생성 16회 · 수정 10회)   │   │ • 인쇄·제본 후 기관으로 배송  │
 │ • PDF 동화책 다운로드       │   │                         │
 │ • 오디오북 다운로드         │   │  [세로형 포토북 기준]        │
 └─────────────────────────┘   └─────────────────────────┘

 ┌ 토큰 안내 ────────────────────────────────────────────┐
 │ 그림 1장 = 토큰 10개   ·   동화책 1권 = 토큰 260개        │
 └──────────────────────────────────────────────────────┘
 ※ 가격 확정 전 초안이며, 부가세 포함 여부와 배송비 기준은 확정 후 반영합니다.
```
- 가격 숫자는 진입 시 카운트업
- 책제작 포함 가격은 `books`에서 `default: true`인 항목 가격을 표시
- 하단 토큰 안내 줄의 수치는 모두 CONFIG 계산값
- 인터랙션: 토큰 안내에 마우스를 올리면 툴팁 "(생성 16 + 수정 10) × 10 = 260"

---

### S5. 실물 동화책 형태를 고를 수 있어요

```
 실물 동화책 형태를 고를 수 있어요
 책제작 포함 이용권 1인당 가격 (디지털 구성 포함)

 ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
 │ [책 SVG]  │ │ [책 SVG]  │ │ [책 SVG]  │ │ [책 SVG]  │
 │ 정사각 소형 │ │ 세로 판형  │ │ 얇은 중철  │ │ 넓은 정사각 │
 │          │ │  기본     │ │          │ │ 프리미엄   │
 │ 인스타북   │ │ 세로형     │ │ 포토진    │ │ 정방형     │
 │ 설명       │ │ 포토북     │ │ 설명      │ │ 포토북     │
 │ 18,000원  │ │ 20,000원  │ │ 23,000원  │ │ 30,000원  │
 └──────────┘ └──────────┘ └──────────┘ └──────────┘

  기관 프로그램 성격에 맞춰 학급방 단위로 한 가지 형태를 정하는 것을 권해요.
```
- 4열 카드. 책 SVG는 **판형 비율이 다르게** 보이도록 그린다 (정사각 소형 / 세로 / 얇은 세로 / 큰 정사각).
- 기본 항목(세로형)은 코랄 테두리 + `기본` 배지로 선택 상태 표시.
- 인터랙션: 카드 클릭 시 선택 상태가 바뀌고, **선택값은 S6 계산기의 책 형태 기본값으로 이어진다.**
- 카드 순서: `CONFIG.books` 배열 순서.

---

### S6. 학급방 규모별 예산 계산 (핵심 인터랙션)

```
 우리 기관 예산을 계산해 보세요
 참여자 수와 책 형태를 고르면 바로 계산돼요

 ┌ 입력 ──────────────────────┐  ┌ 결과 ─────────────────────────┐
 │ 참여 인원                   │  │ 디지털 이용권                   │
 │ [ - ]  20명  [ + ]          │  │ 80,000원           (초록)      │
 │ ───●──────────── 슬라이더    │  │ 4,000원 × 20명                 │
 │ 3            100          │  │                               │
 │                           │  │ 책제작 포함 이용권 · 세로형        │
 │ 책 형태                     │  │ 400,000원          (코랄)      │
 │ (●세로형)(인스타)(포토진)(정방형)│  │ 20,000원 × 20명                │
 │                           │  │                               │
 │ 빠른 선택                   │  │ 필요 토큰 5,200개               │
 │ [3명][10명][20명][30명]      │  └───────────────────────────────┘
 └───────────────────────────┘

 ┌ 규모별 비교 막대 ───────────────────────────────────────────┐
 │  3명  ▇ ▇▇▇                                               │
 │ 10명  ▇▇ ▇▇▇▇▇▇▇▇                                         │
 │ 20명  ▇▇▇ ▇▇▇▇▇▇▇▇▇▇▇▇▇▇  ← 현재 선택 인원 강조               │
 │ 30명  ▇▇▇▇ ▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇▇                            │
 └───────────────────────────────────────────────────────────┘
```
| 기능 | 요구사항 |
|---|---|
| 인원 입력 | 슬라이더 3~100, `-`/`+` 버튼, 직접 입력. **3명 미만 입력 시 3으로 보정**하고 "학급방은 3명 이상일 때 만들 수 있어요" 안내 |
| 책 형태 | 칩 4개 단일 선택, 기본값은 S5 선택값 |
| 빠른 선택 | `budgetExamples` 값 버튼 |
| 결과 | 디지털·책제작 포함 금액, 산식(단가 × 인원), 필요 토큰 수 |
| 비교 막대 | 가로 막대 2색(초록·코랄). 선택 책 형태 가격으로 재계산. 현재 인원이 예시 값과 같으면 해당 행 강조 |
| 반응 | 입력 변경 즉시 계산, 금액 숫자 300ms 카운트 전환 |
| 기본 상태 | 인원 20명, 세로형 |

기준값 검증(세로형):
| 인원 | 디지털 | 책제작 포함 |
|---|---|---|
| 3명 | 12,000원 | 60,000원 |
| 10명 | 40,000원 | 200,000원 |
| 20명 | 80,000원 | 400,000원 |
| 30명 | 120,000원 | 600,000원 |

---

### S7. 마무리

```
┌─────────────────────────────────────────────────────┐
│ ■ dark 배경                                          │
│                                                     │
│  우리 기관의 작은 작가들,                               │
│  Tstory.ai와 함께 시작해요                              │
│                                                     │
│  학급방 개설 · 견적 · 단체 신청 문의                      │
│  틸론(Tilon) Tstory.ai 담당                            │
│  [ 연락처 · 이메일 자리 ]                                │
│                                                     │
│  [ 처음으로 ]  [ 예산 다시 계산하기 ]                     │
└─────────────────────────────────────────────────────┘
```
- 버튼: 처음으로 → S1, 예산 다시 계산하기 → S6
- 연락처는 `CONFIG.contact`로 분리 (확정 전 빈칸 표시)

---

## 8. 부가 화면

### 8.1 개요 보기 (`O`)
- 전체화면 오버레이, 4열 썸네일 그리드 (실제 슬라이드를 축소 렌더링)
- 현재 슬라이드 테두리 강조, 클릭 시 이동, `Esc`로 닫기

### 8.2 발표자 노트 (`N`)
- 화면 하단 30% 높이 패널, 슬라이드는 위로 축소
- 각 슬라이드 `<aside class="notes">` 내용 표시
- 노트 초안:

| 슬라이드 | 노트 |
|---|---|
| S1 | 오늘은 기관에서 AI동화책을 운영하는 방법과 비용을 순서대로 안내드립니다. |
| S2 | 글은 아이가 직접 쓰고 AI는 그림을 돕습니다. 디지털과 책제작 포함, 두 가지로 이용할 수 있습니다. |
| S3 | 담당자가 방을 만들고 링크만 공유하면 됩니다. 참여자 3명부터 운영할 수 있습니다. |
| S4 | 1인당 디지털 4,000원, 실물 책 포함 20,000원입니다. 한 권에 그림 생성 16회와 수정 10회가 포함됩니다. |
| S5 | 프로그램 성격에 따라 책 형태를 고를 수 있고, 기본은 세로형 포토북입니다. |
| S6 | 여기 계신 기관의 인원을 넣어 바로 계산해 보겠습니다. |
| S7 | 학급방 개설과 단체 견적은 담당자에게 문의해 주세요. |

### 8.3 인쇄 (Ctrl+P)
- `@media print`: 슬라이드 1장 = A4 가로 1페이지, 하단 UI·노트 숨김, 애니메이션 없이 최종 상태 출력
- S6은 기본 상태(20명·세로형)로 출력

---

## 9. 반응형

| 뷰포트 | 동작 |
|---|---|
| 1280px 이상 | 16:9 캔버스를 화면에 맞춰 스케일, 좌우 여백은 dark 배경 |
| 768~1279px | 동일 스케일, 하단 UI 버튼 크기 확대 |
| 767px 이하 (세로 모바일) | **스크롤 문서 모드**로 전환: 슬라이드를 세로로 나열, 카드 1열 배치, 계산기 입력/결과 세로 배치, 좌우 스와이프 대신 스크롤 |

---

## 10. 접근성

- 각 슬라이드 `<section aria-label="3 / 7 학급방 운영">`
- 슬라이드 변경 시 `aria-live="polite"` 영역에 "3 / 7, 학급방으로 한 번에 운영해요" 안내
- 버튼에 `aria-label`, 계산기 입력에 `<label>` 연결
- 텍스트 대비 4.5:1 이상 (코랄 `#E9785A` 위에 흰 글자는 큰 글씨에만 사용)
- 색상만으로 이용권을 구분하지 않고 라벨 텍스트를 함께 표시

---

## 11. 수용 기준 (체크리스트)

- [ ] `index.html` 파일 하나를 더블클릭하면 외부 서버 없이 열린다 (폰트 CDN 실패 시 대체 폰트로 표시)
- [ ] 키보드·버튼·스와이프로 7장을 모두 이동할 수 있다
- [ ] `#/6`으로 접속하면 S6이 열린다
- [ ] `CONFIG.plans.digital.price`를 5000으로 바꾸면 S4, S6, 노트를 제외한 모든 화면 숫자가 바뀐다
- [ ] S5에서 포토진을 선택하면 S6의 기본 책 형태가 포토진이다
- [ ] S6에 인원 2를 입력하면 3으로 보정되고 안내 문구가 나온다
- [ ] S6 세로형 20명 결과가 디지털 80,000원, 책제작 포함 400,000원, 토큰 5,200개다
- [ ] 계산기 입력 중 방향키를 눌러도 슬라이드가 넘어가지 않는다
- [ ] 767px 이하에서 스크롤 문서 모드로 표시되고 가로 스크롤이 생기지 않는다
- [ ] 인쇄 미리보기에서 7페이지로 출력된다
- [ ] 문구에 '무료' 표현이 없다

---

## 12. 바이브코딩 진행 순서 (프롬프트 가이드)

한 번에 모두 요청하지 말고 단계별로 나눠 요청한다. 각 단계 시작 시 이 문서를 함께 첨부한다.

**1단계 — 뼈대**
> 첨부한 PRD의 3·4·5·6장을 기준으로 단일 index.html을 만들어 줘. CONFIG 객체, 색상 CSS 변수, 16:9 스케일 캔버스, 슬라이드 7개 빈 섹션, 키보드·버튼 이동, 진행 바, 해시 URL까지만 구현해 줘.

**2단계 — 정적 슬라이드**
> 7장 화면구성의 S1~S5, S7을 구현해 줘. 모든 숫자는 CONFIG에서 가져오고, 책 SVG는 판형 비율이 다르게 보이게 그려 줘.

**3단계 — 예산 계산기**
> S6을 구현해 줘. 입력 규칙, 3명 보정, 비교 막대, S5 선택값 연동, 계산기 포커스 중 단축키 비활성화를 PRD대로 적용해 줘.

**4단계 — 발표 기능**
> 8장의 개요 보기, 발표자 노트, 전체화면, 단계 공개 모드(S3), 진입 애니메이션과 reduced-motion 대응을 추가해 줘.

**5단계 — 마감**
> 9·10장의 반응형·접근성과 인쇄 스타일을 적용하고, 11장 수용 기준을 하나씩 점검한 결과를 표로 알려 줘.

---

## 13. 확정 필요 사항

| 항목 | 현재 상태 | 영향 화면 |
|---|---|---|
| 부가세 포함 여부 | 미확정 (초안 문구 표시) | S4, S5, S6 |
| 배송비 기준 | 미확정 | S4, S6 |
| 1권 생성·수정 횟수 | PPT 기준 생성 16회·수정 10회 (이전 논의안은 22장·수정 4회) | S2, S4, S6 |
| 문의 연락처 | 미정 | S7 |
| 계산기 인원 상한 | 100명으로 가정 | S6 |
| 토큰 안내·제작 배송·체크리스트 슬라이드 | 최신 PPT에서 제외됨 → 발표자료에도 제외 | 추가 여부 결정 |
